// One-time import of the 66 backlog items that previously lived only on
// an external Claude Artifact board (https://claude.ai/code/artifact/
// 43333269-4f57-4dd3-8e44-c72367c2525d). This file is the historical
// snapshot as of 2026-09-10 — the BacklogItem table is the source of
// truth going forward; do not re-run this seed after the initial import.
export interface BacklogSeedItem {
  number: number;
  title: string;
  description: string;
  category: string;
  priority: string;
  status: string;
  dateAdded: string;
  actualFixDate: string | null;
}

export const BACKLOG_SEED_DATA: BacklogSeedItem[] = [
  { number: 1, title: 'Resend-verification endpoint', description: 'DONE 2026-09-08: POST /api/auth/resend-verification (mints a fresh token + fresh 24h expiry, invalidates the old one; 404 unknown email, 400 already-verified, 200 otherwise). LoginPage shows a "Resend verification email" button when login fails with the existing 403 "not verified" error, reusing the typed-in email. VerifyEmailPage\'s error state (previously a dead end) now links back to login. Smoke-tested live end to end: registered a tenant, back-dated its token expiry to simulate a real expired link, confirmed the old token correctly rejected, resent, confirmed a genuinely new token, verified successfully with it.', category: 'Feature', priority: 'High', status: 'Done', dateAdded: '2026-09-07', actualFixDate: '2026-09-08' },
  { number: 2, title: 'Deploy landing page to production VPS', description: 'DONE 2026-09-07: deployed as barkie-landing.service on the VPS (Express + better-sqlite3, port 4100), nginx reverse-proxies barkie.co.za/www.barkie.co.za to it -- same pattern as lapanza-admin.service. Existing Certbot SSL untouched. Verified live including /api/notify signup end-to-end.', category: 'Feature', priority: 'High', status: 'Done', dateAdded: '2026-09-07', actualFixDate: '2026-09-07' },
  { number: 3, title: 'Deploy Barkie API foundation to a real server', description: 'DONE 2026-09-07: deployed to the VPS as barkie-api.service (systemd, port 4200, behind nginx reverse proxy), barkie_prod Postgres 16 database provisioned. Live at https://barkie.co.za/api/.', category: 'Feature', priority: 'High', status: 'Done', dateAdded: '2026-09-07', actualFixDate: '2026-09-07' },
  { number: 4, title: 'Confirm final site name & domain', description: 'SRS §10 open item: "Barkie" / barkie.co.za are still working-title placeholders per the original spec. Business owner decision needed before it\'s treated as final everywhere (repo names, branding, legal docs).', category: 'Feature', priority: 'Medium', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 5, title: 'Finalize brand assets (logo, palette, fonts)', description: 'SRS §10 open item. Landing page currently evolves the lapanza-3d-fullsite design tokens (Fraunces/DM Sans, terracotta/charcoal/cream) as a placeholder direction, not a signed-off Barkie identity.', category: 'Feature', priority: 'Medium', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 6, title: 'Unmatched API routes return 401 instead of 404', description: 'customersRouter is mounted with no path prefix and requireTenantAuth has no path, so every request that falls through health/auth hits the auth middleware (and a session DB lookup) before a real 404 handler ever runs.', category: 'Bug', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 7, title: "TRUST_PROXY only recognizes the exact string 'true'", description: "DONE: env.ts exports trustProxyWarning(raw), called once at module load -- logs a clear warning if TRUST_PROXY is set to anything other than exactly 'true'/'false'/unset. Exact-match behavior itself unchanged (still === 'true').", category: 'Bug', priority: 'Medium', status: 'Done', dateAdded: '2026-09-07', actualFixDate: '2026-09-08' },
  { number: 8, title: 'authLimiter shares one bucket across register + login', description: 'DONE: split into loginLimiter (login only) and accountLimiter (register + resend-verification, added later this session). Same 10/hour window/limit values -- this was about bucket isolation, not re-tuning thresholds.', category: 'Enhancement', priority: 'Medium', status: 'Done', dateAdded: '2026-09-07', actualFixDate: '2026-09-08' },
  { number: 9, title: 'Login timing side-channel', description: 'verifyPassword (slow bcrypt) only runs when a tenant is found, so response latency differs between unknown-email and wrong-password even though the JSON body is identical. Judged acceptable for a B2B login; revisit in a security pass.', category: 'Tech Debt', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 10, title: '"No any" constraint not enforced by tooling', description: 'tsc --noEmit (added this pass) checks types but doesn\'t flag explicit any. No ESLint config exists. If the no-any rule matters, needs @typescript-eslint/no-explicit-any with the two justified test-file exemptions.', category: 'Tech Debt', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 11, title: 'tsconfig outDir is vestigial', description: 'outDir: dist is set but no build script exists (start uses tsx directly) and rootDir was dropped once tests/ joined the project -- a future tsc build would emit dist/src/** + dist/tests/** unexpectedly. Drop outDir or split a tsconfig.build.json when a real build step is added.', category: 'Tech Debt', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 12, title: 'Expired sessions are never pruned', description: 'DONE: getSession() now deletes an expired session row on detection (delete-on-detection, best-effort -- a failed prune logs and returns null rather than 500ing what should be a clean 401). No scheduled job needed; self-prunes as expired sessions are naturally encountered by real traffic.', category: 'Tech Debt', priority: 'Medium', status: 'Done', dateAdded: '2026-09-07', actualFixDate: '2026-09-08' },
  { number: 13, title: 'No updatedAt on Customer (or future models)', description: 'Customer supports PATCH but only records createdAt. Cheap to add now (@updatedAt @db.Timestamptz(3)), expensive to backfill later once real data exists.', category: 'Tech Debt', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 14, title: 'PATCH /api/customers/:id should return the updated record', description: "Currently returns only {ok:true} (a consequence of updateMany, which can't return the row), forcing clients into a follow-up GET. Worth revisiting when the tenant-scoped wrapper grows.", category: 'Enhancement', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 15, title: 'Phase 1: Printer & preset management module', description: 'DONE 2026-09-07: Printer CRUD + nested Presets + append-only Maintenance Log, all tenant-scoped, all with real cross-tenant isolation tests. Merged to master.', category: 'Feature', priority: 'High', status: 'Done', dateAdded: '2026-09-07', actualFixDate: '2026-09-07' },
  { number: 16, title: 'Phase 1: Filament library module', description: 'DONE 2026-09-07: Filament CRUD, tenant-scoped, diameter restricted to 1.75/2.85mm. Merged to master.', category: 'Feature', priority: 'High', status: 'Done', dateAdded: '2026-09-07', actualFixDate: '2026-09-07' },
  { number: 17, title: 'Phase 1: Labour steps & rates module', description: 'DONE 2026-09-07: Labour Step CRUD, tenant-scoped, active defaults true. Merged to master.', category: 'Feature', priority: 'High', status: 'Done', dateAdded: '2026-09-07', actualFixDate: '2026-09-07' },
  { number: 18, title: 'Phase 1: Consumables management module', description: 'DONE 2026-09-07: Consumable CRUD, tenant-scoped, category enum validated. Merged to master.', category: 'Feature', priority: 'High', status: 'Done', dateAdded: '2026-09-07', actualFixDate: '2026-09-07' },
  { number: 19, title: 'Phase 1: Print job costing engine', description: 'DONE 2026-09-07: pure Decimal-based calculation engine (filament/electricity/depreciation/labour/consumables/markup) + CostingTemplate persistence, resolving all four reference types tenant-scoped. Went through 3 rounds of final-review fixes, all money-correctness bugs (rounding order, snapshot-rate reconciliation, silent zero-cost on missing printer wattage) -- each live-reproduced and closed. Merged to master, 86 tests passing.', category: 'Feature', priority: 'Critical', status: 'Done', dateAdded: '2026-09-07', actualFixDate: '2026-09-07' },
  { number: 20, title: 'Phase 1: Quotes & invoices', description: 'DONE 2026-09-08: Quote builder (dual-mode line items: costing-template-derived or ad-hoc), VAT applied per tenant\'s registration status, quote-to-invoice conversion (atomic transaction), PDF generation (pdfkit, not react-pdf as originally scoped -- decided during the PDF phase design), real email delivery via Gmail SMTP, invoice payment-status tracking (unpaid/partially_paid/paid/overdue). Full backend + frontend, live in production.', category: 'Feature', priority: 'Critical', status: 'Done', dateAdded: '2026-09-07', actualFixDate: '2026-09-08' },
  { number: 21, title: 'Phase 1: Public shop profile page (/shop/{slug})', description: "Pulled forward from the Gemini brainstorm doc into Phase 1 scope per the business owner's choice: public read-only page (tagline, services, hours, gallery, contact links) + its Settings editor.", category: 'Feature', priority: 'Medium', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 22, title: 'Phase 1: Material selector / comparison library', description: 'Pulled forward from the Gemini brainstorm doc. Global (not tenant-scoped) reference table of materials with recommended temps, ZAR/kg ranges, application-based tags -- a "use this material" link pre-fills a new costing template.', category: 'Feature', priority: 'Medium', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 23, title: 'Phase 1: Kanban production queue (Jobs board)', description: 'Pulled forward from the Gemini brainstorm doc. New Jobs table/board (backlog/slicing/printing/post-processing/done) alongside the Costing Templates list -- a template becomes a job when work starts.', category: 'Feature', priority: 'Medium', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 24, title: 'Phase 1: Subscriber dashboard frontend', description: 'DONE: React + Vite + TS + Tailwind + React Router + React Query SPA. Auth pages (register/login/verify-email/resend-verification), app shell with nav, and a full CRUD UI for every backend module. Live at https://barkie.co.za/app/.', category: 'Feature', priority: 'High', status: 'Done', dateAdded: '2026-09-07', actualFixDate: '2026-09-07' },
  { number: 25, title: 'Phase 2: Subscription & billing automation', description: 'DONE 2026-09-09: 14-day trial, payment captured at trial end, PayFast/PayPal recurring billing, lapsed/read-only account handling. Live in production, PayFast smoke-tested end to end. PayPal has credentials configured but not yet smoke-tested (tracked separately).', category: 'Feature', priority: 'Medium', status: 'Done', dateAdded: '2026-09-07', actualFixDate: '2026-09-09' },
  { number: 26, title: 'Phase 2: Full Platform Admin panel', description: 'DONE 2026-09-10: server-rendered admin panel in platform/api at /api/admin -- tenant list/detail/edit, subscription grant/adjust/cancel, Plan management, link to landing/\'s signup list. Merged, deployed, bootstrapped. Follow-up hardening items tracked separately (#66).', category: 'Feature', priority: 'Medium', status: 'Done', dateAdded: '2026-09-07', actualFixDate: '2026-09-10' },
  { number: 27, title: 'Phase 2: Notifications, newsletter & reports dashboard', description: 'Email notification system (trial ending, low stock, invoice overdue, etc.), third-party newsletter integration (Mailchimp or similar), and the Reports & History overview dashboard.', category: 'Feature', priority: 'Medium', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 28, title: "customers.test.ts's auth test uses the old (pre-fix) pattern", description: "Its 'requires auth' test still asserts against full buildApp() rather than a minimal single-router app -- works today only because customersRouter mounts first in app.ts. The 6 newer resource test files were fixed to build minimal single-router apps instead. Bring customers.test.ts in line.", category: 'Tech Debt', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 29, title: 'Test helper duplication across 6+ resource test files', description: 'buildMinimalApp() and loggedInAgent() are duplicated verbatim across printers.test.ts, printer-presets.test.ts, printer-maintenance.test.ts, filaments.test.ts, labour-steps.test.ts, consumables.test.ts (~120 lines total). Extract both into tests/helpers/testApp.ts, with buildMinimalApp() taking the router as a parameter.', category: 'Tech Debt', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 30, title: 'PATCH can never clear purchaseDate back to null', description: "scoped.ts's printer/filament update() does data.purchaseDate ? new Date(...) : undefined -- an empty string is silently ignored and null is rejected by zod, so once purchaseDate is set on a printer or filament it can never be cleared via PATCH.", category: 'Bug', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 31, title: "suggestedPrice's rounded-markup ordering has no automated test", description: 'The fix that rounds markupPercent before applying it to totalCost (rather than after) is verified correct by hand-trace (a case exists where the two orderings genuinely differ: 218.67 vs 218.66) but no test asserts suggestedPrice specifically for such a discriminating case -- a regression here wouldn\'t be caught.', category: 'Enhancement', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 32, title: 'markupPercent has no upper bound, overflow gives 500 not 400', description: 'createCostingTemplateSchema validates markupPercent as z.number().min(0) with no .max() -- the column is Decimal(6,2), max 9999.99. A markup of 10000 passes zod validation and dies as a Postgres numeric overflow, surfacing as a generic 500 instead of a clean 400.', category: 'Bug', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 33, title: "CostingTemplate doesn't snapshot printer powerDrawWatts", description: "printerSnapshotDepreciationPerHour is stored precisely so depreciation stays reproducible without the live printer record, but the equivalent isn't done for electricity -- the rate is snapshotted, the wattage isn't -- so electricityCost is the one persisted cost component that can't be re-derived purely from the template's own snapshot columns if the printer is later edited or deleted.", category: 'Enhancement', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 34, title: 'N+1 reference lookups in costing-templates route', description: 'The labour-line and consumable-line resolution loops in POST /api/costing-templates await one findById per line sequentially. Correct and tenant-safe, but a findMany({where:{id:{in:ids}}}) per type would be one round trip each -- worth revisiting once quotes/invoices multiply typical line counts.', category: 'Tech Debt', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 35, title: 'No DB-level unique constraint on (tenantId, number) for quotes/invoices', description: 'Document-number uniqueness rests entirely on the TenantSequence counter, not a database constraint. Setting quoteNumberPrefix/invoiceNumberPrefix to the same value produces a quote and an invoice with the identical number string, no violation raised. Add @@unique([tenantId, number]) to Quote and Invoice as defense-in-depth for a statutorily-important invariant (SA VAT sequential numbering).', category: 'Bug', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 36, title: 'Absurd line-item totals can still overflow Decimal(12,2) and burn a document number', description: "unitPrice/quantity are bounded per-field, but the computed subtotal/VAT/total aren't range-checked before tenantSequences.next() is called. A line near the per-field max (or a VAT-registered tenant pushing a near-max subtotal over the cap) overflows Postgres numeric(12,2) on create -- 500, and the already-minted number is never reused. Range-check calculateQuoteTotals()'s result against the column ceiling before minting, in both POST /api/quotes and POST /api/invoices.", category: 'Bug', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 37, title: "PATCH /api/company-profile can't clear vatNumber (or quote/invoice prefixes) back to blank", description: "quoteNumberPrefix/invoiceNumberPrefix/vatNumber now use z.string().trim().min(1), which unconditionally rejects an empty string -- so a tenant de-registering for VAT can never clear a previously-set vatNumber via PATCH (only overwrite it with a new non-blank value). Low priority since the field can still be left unset from day one; only blocks clearing an already-set value.", category: 'Enhancement', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 38, title: "Dead code: company-profile.ts's vatNumber==='' object-refine is unreachable", description: "updateCompanyProfileSchema's .refine(data => !(data.vatRegistered === true && data.vatNumber === '')) can never fire -- vatNumber's own field schema (z.string().trim().min(1)) already rejects an empty string unconditionally, before the object-level refine runs. Harmless but should be deleted for clarity, or the field schema loosened if empty-string clearing is ever wanted (see item 037).", category: 'Tech Debt', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 39, title: 'QuoteLineItem/InvoiceLineItem have no own tenantScope() group and no line-item-level isolation test', description: "Both line-item tables are only reachable via their tenant-filtered parent's include, so isolation is real, but the two isolation tests added for quotes/invoices both create their record with lineItems: [], so even that parent-mediated path is never exercised end to end. Add one non-empty line item to each test.", category: 'Tech Debt', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-07', actualFixDate: null },
  { number: 40, title: "Can't clear email/vatNumber/quoteNumberPrefix/invoiceNumberPrefix back to blank from the frontend", description: "The frontend's blank-field handling (src/lib/omitBlankFields.ts) omits these fields from the PATCH body when left blank, rather than sending '' -- because the backend's zod schemas reject '' for them (.email() on Customer.email; implicit non-blank requirement via the VAT-registered check and .min(1) on the two prefixes). This was a deliberate compromise (the alternative, sending '', would 400) but means a customer's email or a tenant's vatNumber/prefixes can never be cleared once set, only overwritten with a new non-blank value. Real fix needs a backend change: accept '' for these fields and map to null server-side (e.g. z.union([z.string().email(), z.literal('')]).optional() with a transform to null), or add an explicit 'clear' affordance.", category: 'Enhancement', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-08', actualFixDate: null },
  { number: 41, title: 'Clearing an optional numeric field in edit mode silently no-ops (frontend)', description: "Across Filaments (costPerSpool, costPerKg, spoolWeightGrams, remainingWeightGrams, lowStockThresholdGrams) and Consumables (currentStock, reorderThreshold), clearing an already-set optional numeric field to blank and saving sends undefined (correctly, per the null-vs-undefined fix), which is omitted from the PATCH body entirely -- so the server keeps the old value while the UI navigates away implying success. Same family as backlog item 040 (string fields), but broader and not yet documented for numeric fields. Needs either a real 'clear' UI affordance or a backend convention change.", category: 'Enhancement', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-08', actualFixDate: null },
  { number: 42, title: "Frontend form-page test suites don't test the id-keyed populate-once guard", description: "CustomerFormPage.test.tsx has explicit regression tests for the populatedForIdRef guard (populates once, reloads correctly on navigating between two different records' edit pages) -- this is the exact fix for a real bug found during Company Profile's review. FilamentFormPage/LabourStepFormPage/ConsumableFormPage's test suites don't carry the same tests, even though all three use the identical guard pattern. Verified working today by manual/reviewer inspection, but untested -- the guard could regress silently. Backfill the same 2 tests (from CustomerFormPage.test.tsx) into all three suites before the next CRUD phase.", category: 'Tech Debt', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-08', actualFixDate: null },
  { number: 43, title: "Filament list shows blank instead of em-dash for a colour cleared to ''", description: "FilamentsListPage.tsx's colour column uses `filament.colour ?? '—'`, but clearing the colour field sends '' (correct, per the blank-field convention for this field), not null -- so the placeholder never actually renders for a filament edited to have no colour, only for one that's never had a value touched via the API directly. Cosmetic; would need `filament.colour || '—'` or backend normalization of '' to null.", category: 'Bug', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-08', actualFixDate: null },
  { number: 44, title: 'AppShell nav has no active-route indication, and no unauthenticated-redirect test coverage for Filaments/Labour Steps/Consumables routes', description: "AppShell's nav is 6 plain <Link>s with no visual indication of the current page (was fine at 1-3 items, less so at 6, worse as more modules are added -- a NavLink swap with an active-state class is cheap). Separately, tests/App.test.tsx's unauthenticated-redirect coverage stops at /customers; the three newer modules' routes were only tested for their authenticated happy path, not the redirect-to-login case (verified manually working, just untested).", category: 'Enhancement', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-08', actualFixDate: null },
  { number: 45, title: 'No client-side signal that a Printer needs 4 specific fields filled in before it can be costed', description: 'DONE: PrinterFormPage now shows a "Required for job costing" label above the 4 fields (purchase cost, power draw, electricity rate, expected lifetime) that POST /api/costing-templates actually requires. UI hint only -- fields remain optional at the schema/validation level.', category: 'Enhancement', priority: 'Medium', status: 'Done', dateAdded: '2026-09-08', actualFixDate: '2026-09-08' },
  { number: 46, title: 'No min/max bounds on numeric inputs matching backend .nonnegative()/.positive() constraints', description: 'electricityRatePerKwh (.nonnegative()) and expectedLifetimeHours (.positive()) on Printer, and similar constrained fields elsewhere, have no min attribute on their frontend inputs. A negative or zero value where the backend requires positive produces only the generic "Invalid printer fields." 400 with no field-level attribution. Codebase-wide pattern, not printer-specific. Low priority since it\'s a UX papercut, not a data-integrity issue (backend enforces the real constraint).', category: 'Enhancement', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-08', actualFixDate: null },
  { number: 47, title: "Optional numeric fields can't be cleared once set, across every module (broader than backlog item 040)", description: "Every optional-number field's blank state maps to `undefined`, which JSON.stringify drops from the request body entirely -- so clearing an already-set optional number (e.g. Printer's electricityRatePerKwh, Filament's costPerKg, Consumable's reorderThreshold) and saving silently keeps the old server-side value while the UI implies success. Item 040 covers this same class but was scoped to the specific omit-when-blank STRING fields (email/vatNumber/prefixes); this is the general case for every optional NUMBER field in every module. Needs either a real 'clear' UI affordance (e.g. a small x/clear button per field) or a backend convention for explicit null.", category: 'Enhancement', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-08', actualFixDate: null },
  { number: 48, title: 'serializeInvoice should expose a server-computed balance/amountDue field before the Invoices frontend is built', description: 'DONE: serializeInvoice() exposes balanceDue (Prisma.Decimal subtraction, .toFixed(2) at the response boundary -- never a JS float subtraction), added specifically to avoid the frontend ever needing to do this arithmetic itself. Used by InvoiceDetailPage and the generated PDF.', category: 'Enhancement', priority: 'Medium', status: 'Done', dateAdded: '2026-09-08', actualFixDate: '2026-09-07' },
  { number: 49, title: 'formatCurrency has minor edge-case gaps (prototype-chain lookup, no negative-value handling, no thousand separators)', description: "src/lib/formatCurrency.ts's CURRENCY_SYMBOLS[currency] lookup walks the JS prototype chain (formatCurrency('1.00','constructor') returns a function's toString()) -- unreachable today since currency is never user-supplied, but worth Object.create(null) or Object.hasOwn since this helper is now the app-wide money-display primitive every future phase (Quotes, Invoices) reuses. Also: no negative-value formatting (only matters if credit notes/refunds are ever added, no such schema exists yet), and no thousand-separator grouping for larger invoice totals (10+ digit totals are possible per the unitPrice/quantity column caps) -- if added, must be a string transform on the integer part only, never Number(value).toLocaleString(), which would destroy the Decimal scale invariant.", category: 'Tech Debt', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-08', actualFixDate: null },
  { number: 50, title: 'Costing Templates: minor UI polish deferred (no min= on number inputs, no back-link on detail page, list omits createdAt, duplicate line-item field labels)', description: 'From the phase 5 final review, all accepted as non-blocking: (1) weightGrams/printTimeHours/hours/quantity inputs have step="any" but no min="0", so 0/negative values pass HTML validation and hit the backend\'s generic 400 instead of a targeted message; (2) CostingTemplateDetailPage has no "Back to Costing Templates" link; (3) CostingTemplatesListPage doesn\'t show createdAt, which would help disambiguate same-named templates (an immutable snapshot\'s creation date is its natural distinguishing feature); (4) each dynamic labour/consumable line reuses the same field labels ("Hours", "Quantity") across rows with no per-row context for screen readers.', category: 'Enhancement', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-08', actualFixDate: null },
  { number: 51, title: "No way to reach an already-converted quote's invoice from the quote detail page", description: 'The Convert to Invoice button stays visible forever on an accepted quote (accepted is terminal, so nothing hides the button after conversion). Clicking it again correctly 400s ("This quote has already been converted to an invoice", backend transaction+P2002 guard prevents a duplicate), but the user has no link to the invoice that already exists. Quote responses carry no invoiceId/back-reference. Needs a small backend addition (expose the linked invoice\'s id, e.g. via the existing Invoice.quoteId unique relation) before the frontend can add a "View Invoice" link/hide the button post-conversion.', category: 'Enhancement', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-08', actualFixDate: null },
  { number: 52, title: 'Quotes/Invoices: several small polish items deferred from the phase 6 final review', description: 'All accepted as non-blocking: (1) useCustomerLookup() has no isError -- a failed /api/customers fetch renders "Unknown customer" everywhere instead of a distinguishable error state; (2) status shown as raw enum text (partially_paid) on list pages\' filter/rows vs STATUS_LABELS-mapped text ("Partially Paid") on detail pages -- inconsistent; (3) an already-overdue invoice still offers a "Mark as Overdue" self-transition button (faithful to the backend table, but a confusing no-op to a user); (4) no VAT RATE is exposed on quote/invoice responses, only vatApplied (boolean) and vatAmount -- a compliant invoice/PDF needs to print "VAT @ 15%", which means joining against the company profile\'s rate; worth deciding whether serializeQuote/serializeInvoice should carry the rate directly before the PDF phase; (5) the invoice amount-paid input pre-fills at 0.00 for a brand-new unpaid invoice, which is a minor typo-risk data-entry surface (e.g. typing without clearing first) -- an onFocus-select() would mitigate; (6) the add/update(index,patch)/remove(index) line-item state triad is now duplicated 3x (twice in Costing Templates, once in Quotes) with near-identical bodies -- a small useDraftLines<T>(blank) hook would deduplicate once a 4th consumer appears (e.g. an eventual Invoice create form).', category: 'Tech Debt', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-08', actualFixDate: null },
  { number: 53, title: "Quote/Invoice detail pages don't show unitPrice per line or the document's createdAt (issue date)", description: 'Flagged during the phase 6 review as relevant to the upcoming PDF-generation phase: both QuoteDetailPage and InvoiceDetailPage currently show only "description x quantity -> lineTotal" per line (no unit-price column) and never display createdAt anywhere, though a real quote/invoice document needs both (a PDF requires the issue date, and itemized documents conventionally show unit price alongside the line total). Cheap to add now; worth doing before or alongside the PDF phase so the on-screen detail view and the eventual PDF show consistent information.', category: 'Enhancement', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-08', actualFixDate: null },
  { number: 54, title: 'PDF/email send UX: stale success message, ignored devMode flag, duplicated send logic', description: "Deferred from the PDF+email phase final review (all non-blocking): (1) sending a document shows a green success banner, but only handleSend clears it -- an unrelated status action (Mark as Sent, Record Partial Payment, etc.) on the same page leaves the stale 'Emailed to X...' banner on screen; (2) the /send response's devMode boolean is returned and typed but never read -- both detail pages hardcode the dev-mode wording regardless of the flag, so once real SMTP lands both pages will wrongly tell users to check a server console; (3) handleSend and the Send-to-Customer button JSX are duplicated near-verbatim across QuoteDetailPage.tsx and InvoiceDetailPage.tsx, and the SendDocumentResponse type is duplicated verbatim in src/api/quotes.ts and src/api/invoices.ts -- a shared useSendDocument hook or <SendToCustomerButton> component would collapse all of this and make the devMode fix a one-line change instead of two.", category: 'Tech Debt', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-08', actualFixDate: null },
  { number: 55, title: 'PDF generation: residual edge-case hardening (object-URL revoke timing, extreme decimal column width, thin regression coverage)', description: "Deferred from the PDF+email phase final review, after 4 review cycles closed the main pagination corruption bugs (wrapped descriptions, totals-block label wrap, Qty-column regression -- all fixed and empirically verified): (1) downloadPdf.ts calls URL.revokeObjectURL(url) synchronously right after link.click() -- works in Chrome (download starts inside the click dispatch) but has a history of aborting downloads in Firefox/older Safari; defer with setTimeout(...,0); (2) the Total/line-total PDF column (75pt) and the description-only row-height measurement don't account for the Decimal(12,2) ceiling value 'R 9999999999.99' (~79.5pt), which could still wrap and reproduce the same stale-doc.y split class the last 4 fix cycles closed for other columns -- needs either a wider Total column or extending computeRowContentHeight to measure all four cells, not just description; (3) no test asserts a realistic worst-case Qty value (5-8 digit quantities) against the Qty column width as a permanent regression guard, unlike the TOTALS_LABEL_WIDTH guard that exists for totals labels -- this is exactly the gap that let the Qty-column regression slip through once already.", category: 'Tech Debt', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-08', actualFixDate: null },
  { number: 56, title: 'UI and PDF disagree on currency and VAT label', description: 'DONE: QuoteDetailPage/InvoiceDetailPage now use useCompanyProfile()\'s defaultCurrency for every formatCurrency() call instead of the hardcoded ZAR default, and the VAT row reads "VAT (15%)" matching the PDF. Follow-up filed as item 059: the same fix wasn\'t applied to QuotesListPage/InvoicesListPage/CostingTemplatesListPage, so those list pages still hardcode ZAR -- a non-ZAR tenant now sees an inconsistency between list and detail views where before it was uniformly wrong everywhere.', category: 'Bug', priority: 'Low', status: 'Done', dateAdded: '2026-09-08', actualFixDate: '2026-09-08' },
  { number: 57, title: 'No rate limiting on POST /api/quotes|invoices/:id/send', description: 'Flagged in the PDF+email phase final review: express-rate-limit is only applied on src/routes/auth.ts; the new /send routes are unthrottled. pdfkit generation is synchronous CPU work on the event loop, so an authenticated tenant looping /send degrades the API for every other tenant on the single-process VPS deployment. These are the first CPU-heavy endpoints in the app. Authenticated-only, so not urgent, but reusing the existing limiter is a few lines whenever this is picked up.', category: 'Tech Debt', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-08', actualFixDate: null },
  { number: 58, title: 'Real SMTP phase: two test-coverage gaps found in final review', description: "Deferred from the real-SMTP phase final review (both non-blocking, fixes verified correct by manual reasoning + independent re-review, just not pinned by an automated test): (1) QuoteDetailPage.tsx/InvoiceDetailPage.tsx's devMode ternary (real message vs dev-mode message) has no test exercising the devMode:false branch -- existing tests only mock devMode:true, so a regression that always showed dev-mode wording (or the reverse) wouldn't be caught; (2) the /send route tests (quotes.test.ts, invoices.test.ts) run with SMTP unconfigured so only exercise the dev-mode console path -- no test asserts that profile.businessName/profile.email are passed to sendDocumentEmail() in the right order at the route level (a swap would typecheck fine, since both are strings, and wouldn't be caught by any existing test). Cheap fixes: a devMode:false mock case for the frontend tests; a route-level test that mocks mailer.isConfigured()->true and asserts the sendDocumentEmail call args or the resulting email envelope.", category: 'Tech Debt', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-08', actualFixDate: null },
  { number: 59, title: 'List pages (Quotes, Invoices, Costing Templates) still hardcode ZAR after detail pages were fixed', description: "Follow-up from closing item #56 (final review flagged this explicitly): QuoteDetailPage/InvoiceDetailPage now correctly use the tenant's companyProfile.defaultCurrency for money display, but QuotesListPage.tsx, InvoicesListPage.tsx, and CostingTemplatesListPage.tsx still call formatCurrency() with no currency argument (ZAR default). A tenant with a non-ZAR default currency now sees an inconsistency one click apart -- e.g. 'R 100.00' in the quotes list vs 'USD 100.00' on that same quote's detail page -- which is arguably a worse signal than the original bug (everything being uniformly wrong). Fix: thread useCompanyProfile()'s defaultCurrency into these three list pages' formatCurrency() calls too, same pattern as the detail-page fix. Given now 5 call-site groups across 5 pages need this, consider a thin useDisplayCurrency() hook wrapping useCompanyProfile()?.defaultCurrency to make \"forgot the second argument\" structurally harder going forward.", category: 'Bug', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-08', actualFixDate: null },
  { number: 60, title: "PayPal checkout doesn't validate subscription.id exists in the provider response", description: "Flagged in the billing/subscription final review. paypalProvider.ts's createSubscriptionCheckout validates approveLink exists in PayPal's response but not subscription.id. If PayPal ever returned a response missing id (malformed/unexpected shape), providerSubscriptionId would be undefined, and since PayPal events never set the subscriptionId field (only PayFast does, for its own row-id correlation), every future webhook for that subscription would silently resolve to nothing and be dropped -- the exact permanent-no-op failure mode the C1 fix in this phase specifically closed for PayFast. Low likelihood (would require PayPal returning a non-conformant response), one-line guard to close it.", category: 'Bug', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-09', actualFixDate: null },
  { number: 61, title: 'Billing subscription-status banner (AppShell) has zero test coverage', description: 'AppShell.tsx\'s trial/past_due/lapsed banner (day-count arithmetic via Math.ceil on a ms delta, and the conditional copy per status) has no test exercising useSubscription() -- AppShell.test.tsx\'s 3 existing tests don\'t touch it. A regression here (wrong day count, wrong status branching, wrong copy) would ship silently. Cheap to close: wrap the existing tests with a QueryClientProvider + mocked subscription fixture per status, matching the pattern already used in BillingSettingsPage.test.tsx.', category: 'Tech Debt', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-09', actualFixDate: null },
  { number: 62, title: "A lost PayFast trial ITN + resubscribe can leave a live PayFast subscription permanently uncancelable from Barkie", description: "Traced in the final whole-branch review. If a tenant's PayFast trial's first ITN never arrives (lost, or delayed past the 24h slack window), requireActiveSubscription self-heals the row to 'lapsed' with providerSubscriptionId still null (nothing ever bound it). If the tenant then resubscribes, the checkout route's pre-delete best-effort cancel is skipped (guarded on providerSubscriptionId being set), the old row is deleted, and a second PayFast subscription is created. The first PayFast subscription is still live and billing -- but its eventual delayed ITN carries the deleted row's id (the id-scoped webhook correlation this phase introduced), so it resolves to nothing and is silently dropped. Barkie can never learn or cancel that token again; the tenant is billed twice with no in-app way to stop the orphaned one. Needs a product decision, not just a code fix: options include refusing resubscribe on a PayFast-provider row that could still be live without explicit tenant confirmation, or keeping a tombstone of deleted subscription rows so a late ITN can at least surface the orphaned token for manual/support-assisted cancellation. Low likelihood (requires a lost/delayed webhook specifically during the trial window) but real financial impact when it happens.", category: 'Bug', priority: 'Medium', status: 'Backlog', dateAdded: '2026-09-09', actualFixDate: null },
  { number: 63, title: 'No update-payment-method flow -- past_due tenants can only cancel-then-resubscribe', description: "The AppShell past_due banner ('please check your payment method') and the requireActiveSubscription 402 copy ('Update your payment method to continue') both promise a flow that doesn't exist anywhere in the codebase. As of the final-review fixes, a past_due tenant CAN now resubscribe via the checkout route (extended in the same fix pass that added this item), which re-runs a fresh PayFast/PayPal checkout and best-effort-cancels the failing one -- but that's undiscoverable from the banner/error copy, which reads like it should update the existing card, not start over. Either build a real update-payment-method flow (provider-specific -- PayFast and PayPal both support this differently) or, cheaper, just fix the copy to say 'resubscribe' instead of implying an in-place card update exists.", category: 'Feature', priority: 'Medium', status: 'Backlog', dateAdded: '2026-09-09', actualFixDate: null },
  { number: 64, title: 'Billing phase hygiene grab-bag (7 small items from final review)', description: "Small, non-blocking items surfaced across the billing/subscription phase's review cycles, none urgent enough for their own ticket: (1) webhooks.ts's canceled-event-on-canceled-row branch binds providerSubscriptionId as a side effect even though its comment implies it's a pure no-op -- harmless, comment should say so; (2) a dead GRACE_PERIOD_DAYS constant in webhooks.ts, superseded by pastDueSince-based logic, never removed; (3) billing.ts's resubscribe path does providers[existing.paymentProvider] with no guard -- would throw synchronously outside the try/catch if bad data ever got a paymentProvider value outside the payfast/paypal enum (checkout's zod validation makes this unreachable today, but it's a landmine if that ever changes); (4) inconsistent fetch injection between the two provider adapters -- PayFast calls the global fetch directly, PayPal takes an injectable fetchImpl, forcing PayFast's tests to mock globalThis.fetch instead of using the cleaner injection pattern; (5) providerSubscriptionId has no unique index/constraint at the DB level, relying on application logic alone to keep it unique; (6) PayPal's webhook signature verification is handed the re-serialized parsed request body (req.body after express.json()) rather than the raw bytes PayPal originally sent -- a known source of intermittent signature-verification failures industry-wide, worth capturing the raw body for this one route; (7) payfastEncode's own test (added when it was fixed to match PHP's urlencode() exactly) only exercises 4 of the 6 special characters it handles (' ( ) * are covered, ! and ~ are not) -- a future regex regression on just those two characters wouldn't be caught.", category: 'Tech Debt', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-09', actualFixDate: null },
  { number: 65, title: 'Public site follow-ups: materials guide scope, dropdown/keyboard polish', description: "Grab-bag of non-blocking items from the public-site (Home/Pricing/Materials Guide) final review, all fixed for correctness but left as future polish: (1) 9 of the 28 materials in materials-data.js (PLA-CF, Silk/Matte/Wood/Tough PLA, PETG-CF, ASA, PC, HIPS) are marked estimated:false but weren't independently spot-checked against real South African retailer pricing during this build -- only the 4 original worked examples (PLA/PETG/ABS/PEEK) were confirmed grounded; worth a real pricing pass before treating those 9 as verified. (2) The design spec asked for '~24-28 FDM filaments + resin variants' -- the shipped dataset is 28 FDM filaments and zero resin variants; add resin (standard/tough/flexible) if the Materials Guide should cover SLA/resin printing too. (3) The Head-to-head comparison's two material dropdowns are plain <select> elements relying on native browser type-ahead rather than the searchable/filter-as-you-type UX the original competitor reference had -- fine for 28 materials, worth revisiting if the dataset grows much larger. (4) The Materials Guide's 3-tab switcher (Selector/All materials/Head to head) uses role=tablist/tab but has no roving tabindex, no arrow-key navigation, and doesn't move focus into the newly-shown panel on switch -- basic ARIA roles are correct, full keyboard tablist semantics are not implemented.", category: 'Tech Debt', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-09', actualFixDate: null },
  { number: 66, title: 'Admin grant-route hardening + test coverage', description: "From the admin center's final whole-branch review (2026-09-09), 4 Minor, non-blocking findings in the subscription grant/adjust routes (platform/api/src/routes/admin.ts): (1) the grant route's best-effort provider-cancel-before-delete does `providers[existing.paymentProvider]` with no `!provider` guard, unlike the cancel route 100 lines below it -- an unrecognized paymentProvider value would throw before the .catch() swallow attaches, turning a 'never blocks the grant' path into a 500; (2) `planId` is never validated to exist/be active before the irreversible provider-cancel call runs, so a hand-crafted bad planId cancels a tenant's real subscription at the provider then rolls back locally on the FK violation, leaving a locally-active row with a dead provider-side id; (3) an admin saving the adjust form with status='active' and no explicit date now always pushes currentPeriodEnd out 30 days, even for an already-current paying subscription -- accepted as a known consequence since the next real webhook resets it anyway, but worth a guard that only extends when the row is actually stale; (4) none of the 3 Important fixes from this review round (provider-cancel-before-delete, transactional delete+create, currentPeriodEnd default-forward) have direct test coverage -- only the pre-existing cancel-route provider-mock test exists. Low priority, single-operator admin surface.", category: 'Tech Debt', priority: 'Low', status: 'Backlog', dateAdded: '2026-09-09', actualFixDate: null },
];
